```markdown
## Build System

### Package Manager
- **Using Bun** (not npm)
  - Build command: `bun run build`
  - Replace any `npm` commands with `bun` equivalents
  
### Quick Reference
| Task | Command |
|------|---------|
| Build | `bun run build` |
| Install | `bun install` |
| Run scripts | `bun run <script>` |

> ⚠️ **Note**: Always use `bun` commands instead of `npm` for this project
```

_Added on 11/9/2025, 2:23:20 AM GMT-4_

```markdown
## Build Commands

### Package Manager
- **Using Bun** instead of npm
  - Build command: `bun run build`
  - ~~Not~~ `npm run build`

### Quick Reference
```bash
# Correct
bun run build

# Incorrect (don't use)
npm run build
```

**Note:** All npm commands should be replaced with their Bun equivalents throughout the project.
```

_Added on 11/9/2025, 2:23:21 AM GMT-4_

```markdown
## Build Command

**Package Manager: Bun**

Use `bun run build` instead of `npm run build` for all build operations.

- This project uses Bun as the JavaScript runtime and package manager
- Replace any npm commands with their Bun equivalents:
  - `npm install` → `bun install`
  - `npm run [script]` → `bun run [script]`
  - `npm run build` → `bun run build`
```

_Added on 11/9/2025, 2:23:23 AM GMT-4_

```markdown
## Build Commands

**Package Manager**: We use Bun (not npm)

### Build Script
```bash
bun run build
```

**Note**: Always use `bun` commands instead of `npm` equivalents throughout this project.

Common replacements:
- `npm install` → `bun install`
- `npm run [script]` → `bun run [script]`
- `npm start` → `bun start`
```

_Added on 11/9/2025, 2:23:23 AM GMT-4_